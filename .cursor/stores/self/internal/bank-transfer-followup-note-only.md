# CANCELLED — bank transfer follow-up-only strip

## Status
**Cancelled / not applied.** Simona reversed: keep the full `#bank-transfer-details` panel.

## What happened
- Strip request was inspected only (CSS/HTML/JS on `index.html` + `poruchka.html`).
- **No edits, no commit, no push** for the strip.
- Full panel from `383b8c6` remains on `main`: company, ЕИК, IBAN, ОББ, BIC, основание, plus proforma email note.

## Keep as-is
Panel under Банков превод (`payment_id=7`), toggled by `syncPayment()`.
